spring:
  application:
    name: smart-home-iot

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

  profiles:
    active: local